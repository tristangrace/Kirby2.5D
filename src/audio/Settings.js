/** Volume settings the ported audio engine reads (0..1). Kept here so the engine needs nothing else from the 3D repo. */
export const Settings = {
  masterVolume: 0.8,
  musicVolume: 0.55,
  sfxVolume: 0.9,
};
